package CollectionsFramework;

import java.util.*;

public class CollectionsDemo {
    public static void main(String[] args) {
        List<String> list = new ArrayList<>();
        Set<String> set;
        Queue<String> queue;

        // Map doesnt extend Collections interface
        Map<Integer,String> map;

        // Collections framework -> Java term for above interfaces and other classes that represent collections
        // Collections -> a utility class (similar to arrays) that has helper ,methods to work with list.set.queue

        // Collection -> a super interface of List, Set, Queue
        Collection c = new ArrayList<>();

        // what is the difference between Collection and collections
    }
}
