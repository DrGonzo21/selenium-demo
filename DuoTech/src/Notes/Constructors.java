package Notes;

public class Constructors {
    // Constructor vs Method
    // 1. no return type
    // 2. name must be the same as the class
    // 3. constructors are called implicitly
    // 4. constructor are used to initialize the instance variables


//    public DefaultConstructor{

      // when no constructor is added to class, java compiler adda s default constructor
      // which has no args and empty body

    }

