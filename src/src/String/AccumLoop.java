package String;

public class AccumLoop {
    public static void main(String[] args) {
        str = str.toLowerCase();

        String newString = "";

        for (int i = 0; i < str.length(); i++) {

            if (str.charAt(i) != 'a' && str.charAt(i) != 'e' && str.charAt(i) != 'i' && str.charAt(i) != 'o' && str.charAt(i) != 'u') {
                newString += str.charAt(i);
                {
                }
                return newString;

            }
        }
    }
}
