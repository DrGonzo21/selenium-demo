package String;

public class Reverse {
    public static void main(String[] args) {
        String nstr = "this is bad";


        for (int i = 0; i < str.length(); i++) {
            char ch = str.charAt(i);
            nstr = ch + nstr;
        }
        System.out.println(nstr);
    }


        return "";
    }

